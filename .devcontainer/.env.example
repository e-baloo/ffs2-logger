# Configuration des variables proxy pour le Dev Container
# Copiez ce fichier en .env et ajustez les valeurs selon votre environnement

# Proxy HTTP/HTTPS
# Utilisez host.docker.internal pour accéder au proxy sur l'hôte Windows
HTTP_PROXY=http://host.docker.internal:9000
HTTPS_PROXY=http://host.docker.internal:9000

# Liste des domaines à exclure du proxy
NO_PROXY=localhost,127.0.0.1,.local,host.docker.internal

# Configuration npm (si nécessaire)
# npm_config_proxy=http://host.docker.internal:9000
# npm_config_https_proxy=http://host.docker.internal:9000

# Configuration git (si nécessaire)
# GIT_PROXY_COMMAND=connect -H host.docker.internal:9000
